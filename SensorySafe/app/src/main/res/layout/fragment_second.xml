<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sensory_light_grey"
    tools:context=".SecondFragment">

    <!-- Top card: "Set your own" heading -->
    <TextView
        android:id="@+id/textview_set_your_own"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="40dp"
        android:background="@color/sensory_topbar_grey"
        android:gravity="center"
        android:padding="16dp"
        android:text="@string/set_your_own_title"
        android:textColor="@android:color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Threshold label -->
    <TextView
        android:id="@+id/textview_threshold_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="@string/automatic_alerter_label"
        android:textColor="@android:color/black"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/textview_set_your_own"
        app:layout_constraintStart_toStartOf="@id/textview_set_your_own" />

    <!-- Slider for alert threshold (61-100 relative dB) -->
    <com.google.android.material.slider.Slider
        android:id="@+id/slider_alert_threshold"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:stepSize="1"
        app:valueFrom="61"
        app:valueTo="100"
        app:value="80"
        app:layout_constraintTop_toBottomOf="@id/textview_threshold_label"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Current threshold value display -->
    <TextView
        android:id="@+id/textview_threshold_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/slider_alert_threshold"
        app:layout_constraintStart_toStartOf="@id/textview_threshold_label" />

    <!-- Save button (reuses existing binding id button_second) -->
    <Button
        android:id="@+id/button_second"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="40dp"
        android:backgroundTint="@color/sensory_primary_blue"
        android:text="@string/save_now"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
